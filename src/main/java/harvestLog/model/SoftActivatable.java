package harvestLog.model;

public interface SoftActivatable {
    boolean isActive();
    void setActive(boolean active);
}


// implement soft ativation #
// implement categroy controller
// then work on add cropfieldmeasure unit (in modify section)
// then display the list of all properties below the add section, where one can modify directly / delete multiple by marking them)
